# Database
DATABASE_URL="postgresql://user:password@localhost:5432/web3db"

# Frontend
FRONTEND_URL="http://localhost:3000"

# Better Auth
BETTER_AUTH_SECRET="your-secret-key-here"
BETTER_AUTH_URL="http://localhost:8000"

# Issuer DID Keys (generate with lib-vc)
ISSUER_DID="did:key:z..."
ISSUER_PRIVATE_KEY_HEX="your-private-key-hex"
ISSUER_NAME="Your University Registrar"

# EAS Configuration
EAS_SCHEMA_UID="0x..." # Schema UID after registration
EAS_PRIVATE_KEY="0x..." # Ethereum private key for attestations (needs Base Sepolia ETH)

# Web3.Storage (for IPFS)
# Run: npx w3 login your@email.com
# (Credentials stored locally by w3up-client)
